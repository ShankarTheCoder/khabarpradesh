# Copy this file to .env (local dev) or set this as a build-time environment
# variable on your hosting platform (Vercel, Netlify, etc.) for production.

# Point this at your deployed backend's public URL + /api
# Example: VITE_API_URL=https://api.khabarpradesh.com/api
VITE_API_URL=http://localhost:5000/api
